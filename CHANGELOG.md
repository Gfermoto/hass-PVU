# Changelog

## v0.7.4

### Исправления

- **humidity_plausible**: нижняя граница `>= 1` → `>= 0`. В очень сухом воздухе (0–0.9%
  RH, зимний климат) датчик влажности считался неактивным, что могло скрыть ECO-проверку
  по влажности.
- **voc_outdoor_plausible / no_outdoor_plausible**: нижняя граница `>= 1` → `>= 0`.
  SGP41/SGP30 и аналоги могут отдавать `0` как корректное значение «очень чистый воздух».
  Исправлена непоследовательность с внутренними датчиками (уже `>= 0` с v0.7.3).

### CI

- `markdownlint-cli2-action`: обновлён с `@v16` до `@v22`.

### Документация

- `ARTICLE_RU.md`: версия обновлена до v0.7.4; уточнено описание ECO vs антизаморозки
  (ECO выключает HVAC до ветки антизаморозки — физически корректно, т.к. нет тяги = нет риска);
  убран AI-паттерн «стоит знать».
- `ROADMAP.md`: добавлены строки v0.7.3 и v0.7.4.
- `LOGIC.md`: добавлена переменная `eco_has_sensors` в таблицу обозначений.
- `CHANGELOG.md`: исправлен исторический `VOC/NO` → `VOC/NOx` в записи v0.2.0.

---

## v0.7.3

### Исправления

- **voc_plausible / no_plausible**: нижний порог плausibility изменён с `>= 1` на `>= 0`.
  Датчики VOC/NOx, сообщающие `0` в чистом воздухе, теперь корректно распознаются
  как активные — ECO-режим больше не игнорирует их как «нереалистичные».
- **presence_mode=custom**: сравнение состояния теперь регистронезависимо (`| lower`).
  Значение `presence_away_state` больше не нужно приводить к нижнему регистру вручную.
- **Alarm label**: исправлена подпись режима Alarm в UI — было `away/vacation/triggered`,
  стало `armed_away/armed_vacation/triggered` (точные имена состояний HA).

### Документация

- `ARTICLE_RU.md`: расшифровка IAQ → «Indoor Air Quality — качество воздуха в помещении»;
  переставлены разделы «Что ещё есть в репо» и «Что в результате» для лучшего нарратива;
  убрано дублирование описания проверки `hvac_modes`/`fan_modes`.
- `LOGIC.md`: добавлена ветка `eco_active` в диаграмму и сводную таблицу Fan-логики.

---

## v0.7.2

### Исправления

- **ECO + fan coupling**: при `eco_active=true` (`desired_hvac=off`) blueprint теперь
  не отправляет команду `fan_mode`. Ранее мог отправляться `fan_mode=low` (воздух чист),
  что на устройствах с coupling fan→HVAC реактивировало установку.
- **fan_stepdown**: сравнение текущего режима вентилятора теперь идёт с настроенными
  значениями `fan_mode_high`/`fan_mode_medium`, а не с хардкодными строками `'high'`/`'medium'`.
  Актуально если fan-режимы переименованы под конкретную интеграцию.
- **hvac_mode_heat**: убрана опция `fan_only` из списка допустимых значений.
  `fan_only` как «режим нагрева» обходит антизаморозку — установка могла уходить
  в вентиляцию при морозе.

### Описания (UX)

- `outdoor_vent_min`: уточнено описание — ниже порога установка переключается в `heat`
  (антизаморозка), а не просто блокируется вентиляция.
- Оба blueprint: исправлено `VOC/NO` → `VOC/NOx` в тексте description.

### CI

- Добавлены `workflow_dispatch`, `concurrency` и `permissions: contents: read` в `validate.yml`.
- Убраны некорректные path-глобы из `yaml-files` конфига yamllint.
- CI-badge добавлен в README.

---

## v0.7.1

### Исправления

- **CO2 правдоподобность**: снижен минимальный порог с `>= 300` до `>= 200` ppm в обоих
  блюпринтах (`pvu.yaml`, `pvu_min.yaml`). Датчик SCD41 при прогреве мог возвращать
  значения 200–299 ppm, что вызывало `co2_active=False`.

---

## v0.7.0

**Архитектурный рефакторинг логики выбора режима HVAC** — на основе анализа реального
принципа работы ПВУ с рекуперацией тепла (Turkov Zenit HECO, пластинчатый рекуператор).

### Концептуальное изменение

Ранее выбор `desired_hvac_mode` строился от **комнатной температуры**: слишком холодно → `heat`,
слишком тепло → `cool`, иначе → `fan_only`. Это неверно для ПВУ, поскольку:

- ПВУ — **вентиляционное** устройство, **не** основная система отопления/охлаждения.
- Отопление помещения обеспечивают радиаторы и тёплый пол.
- Режим `heat` в ПВУ — это подогрев **приточного воздуха** до уставки, а не нагрев помещения.
- Режим `fan_only` — вентиляция с рекуперацией без калорифера (возврат ~85% тепла вытяжного воздуха).

### Новая логика

**`pvu.yaml`:**
```
1. away=off         → off
2. eco_active       → off
3. мороз снаружи    → heat   (подогрев приточного воздуха)
4. жарко внутри     → cool   (при разрешённых условиях)
5. иначе            → fan_only  (вентиляция по умолчанию)
```

**`pvu_min.yaml`** (без ECO):
```
1. away=off         → off
2. мороз снаружи    → heat
3. жарко внутри     → cool
4. иначе            → fan_only
```

**Ключевые улучшения:**

- **Устранён критический баг:** при выходе из ECO режима (воздух загрязнился) установка
  теперь **всегда** запускается в `fan_only`, а не застревает в `off` при температуре
  в "серой зоне" между полосами гистерезиса.
- `fan_only` стал режимом по умолчанию: установка вентилирует постоянно когда нужно
  (ECO выключен или не активен), независимо от температуры в помещении.
- Убрана ветка `indoor < target → heat`: ПВУ не должна использоваться как
  термостат для нагрева помещения — этим занимаются радиаторы.
- Устранена зависимость `humidity_clear` из выбора HVAC-режима: влажность управляет
  только скоростью вентилятора (`air_boost_count`), а не режимом.

### Breaking changes

- **Удалён параметр `temp_hysteresis_off`** (`pvu.yaml` и `pvu_min.yaml`) — он
  применялся только для "зоны возврата" в старой температурной логике, которая
  упразднена. При обновлении существующей автоматизации поле исчезнет из UI без
  последствий для остальных настроек.
- Изменено поведение при `indoor > target + hyst` + `vent_allowed=True` + `cool_allowed=False`:
  ранее → `''` (не менять), теперь → `fan_only` (продолжать вентилировать).

## v0.6.2

- **Fix (`pvu.yaml`, `pvu_min.yaml`):** добавлена ветка `elif not vent_allowed → heat`
  в `desired_hvac_mode`.
  Ранее при грязном воздухе (ECO деактивирован или отключён) + мороз на улице
  (`vent_allowed=False`) + температура внутри выше `target+hysteresis_off` —
  ни одна ветка не срабатывала и установка оставалась выключенной.
  Теперь в этом случае выбирается `heat`: приточный воздух подогревается
  и воздухообмен всё равно происходит, не жертвуя качеством воздуха.

## v0.6.1

- **Fix (`pvu.yaml`):** шаг 0 (`set_temperature`) теперь пропускается когда
  `desired_hvac_mode=off` (ECO режим или `away=off`).
  Ранее blueprint каждый прогон отправлял `climate.set_temperature` на выключенное
  устройство — команда игнорировалась, но `current_target` не обновлялся, что
  постоянно триггерило debug-уведомление (`|current_target - target| > 0.2`).
  Setpoint будет выставлен в первом прогоне после включения устройства.
- **Fix (`pvu.yaml`):** `debug_need_notify` больше не срабатывает по расхождению
  температурной уставки когда `desired_hvac_mode=off`. Устраняет спам
  debug-уведомлений при активном ECO режиме.

## v0.6.0

- Добавлен **ECO режим** (`eco_mode_enabled`) в `pvu.yaml`:
  - Отдельная секция настроек «🍃 ECO режим» с пятью индивидуальными порогами:
    `eco_co2_max`, `eco_pm25_max`, `eco_voc_max`, `eco_no_max`, `eco_humidity_max`.
  - Если ECO включён и все активные датчики ниже своих ECO-порогов → `hvac_mode=off`.
  - Датчики, не сконфигурированные в основной секции, в ECO-проверке не участвуют.
  - **Edge case:** если ни один датчик воздуха не задан, ECO не активируется
    (`eco_has_sensors=false`) — без контроля воздуха выключать установку
    нет смысла.
  - При ухудшении воздуха (хотя бы один датчик выше порога) включается стандартная
    логика (включая антизаморозку и `outdoor_temp_policy`) без каких-либо изменений.
  - ECO применяется при `away_behavior=maintain`; если установка уже выключена
    по политике away, ECO на это не влияет.
  - Использует существующий `hvac_min_switch_minutes` как anti-flap — отдельного
    интервала нет.
- Добавлены ECO-флаги в `debug_mode`: `eco_active`, `eco_has_sensors`, `eco_all_ok`,
  `eco_co2_ok`, `eco_pm25_ok`, `eco_voc_ok`, `eco_no_ok`, `eco_humidity_ok`.
- **Fix (`pvu.yaml`):** упрощён `skip_hvac_min_interval_for_off` — убрана избыточная
  проверка `desired_hvac_mode == 'off'` (условие уже гарантировано контекстом).
- **Fix (`pvu_min.yaml`):** при `away=off` больше не отправляется `fan_mode=off`.
  Это защищает устройства с coupling fan→HVAC от неявного повторного выключения HVAC.
  Поведение эквивалентно `hvac_only` из полной версии.

## v0.5.3

- Добавлены явные политики для `away_behavior=off` в `pvu.yaml`:
  - `away_off_hvac_policy` (`respect_min_interval` / `immediate`) —
    применение `off` относительно anti-flap интервала;
  - `away_off_fan_policy` (`hvac_only` / `follow_fan_mode`) —
    защита от coupling `fan_mode=off -> HVAC off` на части устройств.
- Усилен `debug_mode`: добавлен `command_verify_delay_sec` и post-check
  после `climate.set_hvac_mode` / `climate.set_fan_mode` при несовпадении
  state с целевым.
- Обновлены `README.md` и `docs/FAQ.md`: спецификация `away=off`,
  диагностика coupling-кейсов.
- Обновлён `docs/RELEASE_CHECKLIST.md`: интеграционные проверки
  `away=off -> disarmed -> armed_away`, пункт по device coupling.
- Полевой beta-test: pass по сценариям `home/away`, anti-flap, day/night;
  зафиксировано ограничение `cool/fan_only` устройством/интеграцией зимой.

## v0.5.2

- Hotfix: из `pvu.yaml` удалён фильтр `split` (ошибка
  `No filter named 'split'`, блокировка сохранения автоматизации).
- Проверка `hvac_modes` / `fan_modes` переписана без `split`,
  устойчиво к list/строковым форматам атрибутов.
- Исправлен импорт автоматики Turkov AirFlow в средах HA,
  где `split` недоступен.

## v0.5.1

- Hotfix регрессии: устранён ложный `unsupported_mode` после выхода из
  `away=off` (HVAC не возвращался в `heat`).
- Проверка `hvac_modes` / `fan_modes` устойчива к разным форматам
  атрибутов (list, строка, enum-подобные).
- Обновлён `docs/RELEASE_CHECKLIST.md`: проверки формата режимов,
  smoke-сценарий `away=off -> disarmed -> heat`.

## v0.5.0

- Реализован Этап 3 из `docs/ROADMAP.md`.
- В `pvu.yaml` добавлены `climate_profile` (continental/mild/cold):
  температурная стратегия, гистерезис, outdoor-ограничения.
- В `pvu.yaml` добавлены `home_profile` (apartment/house/office):
  чувствительность порогов IAQ и влажности.
- Дефолты под загородный дом, климат МО: `home_profile=house`,
  `climate_profile=continental`, гистерезис 1.2/0.6,
  `outdoor_cool_min=20`, `outdoor_vent_min=3`.
- Расширена диагностика `debug_mode`: вывод активных профилей и эффективных порогов.
- В `docs/RELEASE_CHECKLIST.md` добавлены обязательные сезонные smoke-test сценарии (`зима`/`межсезонье`/`лето`) как регулярная практика.

## v0.4.2

- Закрыт Этап 2 из `docs/ROADMAP.md` полностью.
- Добавлен готовый пакет KPI для верификации порогов на реальных данных: [`airflow_kpi_package.yaml`](./airflow_kpi_package.yaml).
- Добавлены шаблоны для режима `Manual card`: [`airflow_card.yaml`](./airflow_card.yaml) и [`airflow_card_min.yaml`](./airflow_card_min.yaml).
- Обновлены инструкции в `README.md`, `docs/TUNING.md` и `docs/FAQ.md` (импорт dashboard vs manual card, устранение `No card type configured`).

## v0.4.0

- Реализован Этап 2 из `docs/ROADMAP.md` (часть, которую можно закрыть в коде и UX).
- В `pvu.yaml` добавлен антидребезг HVAC: параметр `hvac_min_switch_minutes` и защита от частых переключений (`min_interval_hold`).
- Для вентилятора реализован плавный спад скорости (`fan_stepdown_enabled`): `high -> medium -> low` при очистке воздуха.
- Расширен `debug_mode`: добавлены явные причины неприменения режима (`desired_empty`, `already_set`, `unsupported_mode`, `min_interval_hold`, `climate_unavailable`).
- Уточнены `README.md` и `docs/ROADMAP.md` под новую логику Этапа 2.

## v0.3.4

- Выполнен Этап 1 из `docs/ROADMAP.md` (low-hanging fruits).
- В `pvu_min.yaml` добавлен backport `outdoor_temp_policy` (`conservative`/`permissive`) с безопасным поведением при неизвестной наружной температуре.
- В `pvu.yaml` и `pvu_min.yaml` добавлена фильтрация нереалистичных значений датчиков (температура, влажность, CO2, PM2.5, VOC, NOx).
- Уточнён fallback применения режимов: если рассчитанный режим не поддерживается устройством, сохраняется текущий режим.
- В `README.md` добавлен раздел по edge-cases и fail-safe-поведению.
- Добавлен `docs/FAQ.md` с ответами на типовые вопросы по эксплуатации.

## v0.3.3

- Опубликовал дорожную карту развития проекта: [`docs/ROADMAP.md`](./docs/ROADMAP.md).
- `README.md` дополнен ссылкой на roadmap в разделе «Разработка и качество».

## v0.3.2

- Добавлена инфраструктура качества и сопровождения:
  - `CONTRIBUTING.md` с правилами внесения изменений;
  - шаблоны issues (`bug_report`, `feature_request`) и конфиг issue-трекера;
  - `docs/RELEASE_CHECKLIST.md` для проверки перед релизами;
  - CI workflow `validate.yml` (Markdown/YAML lint).
- Добавлен `hacs.json` для подготовки репозитория к HACS-потоку.
- В `README.md` добавлен раздел «Разработка и качество» со ссылками на процессы и проверки.

## v0.3.1

- Для develop-версии `pvu.yaml` все `input sections` переведены в `collapsed: true` по умолчанию.
- Улучшен UX мастера настройки: форма открывается в компактном виде, секции разворачиваются по мере необходимости.

## v0.3.0

- Введено разделение blueprint на две ветки: `pvu_min.yaml` (стабильная база) и `pvu.yaml` (развиваемая версия).
- Добавлены метаданные blueprint: `author` и `homeassistant.min_version`.
- Усилен runtime fail-safe:
  - добавлена проверка доступности climate-сущности перед сервис-вызовами;
  - проверка поддерживаемых режимов переведена на нормализованное сравнение списков (без подстрочного `find`).
- Ускорена реакция автоматики: добавлены триггеры на опциональные IAQ-датчики и presence-источник.
- Добавлена политика на случай отсутствия наружной температуры (`outdoor_temp_policy`):
  - `conservative` (по умолчанию) блокирует `cooling/fan_only` при неизвестной `temp_outdoor`;
  - `permissive` сохраняет прежнее поведение.
- Добавлен `debug_mode` с диагностическими `persistent_notification` для трассировки решений автоматики.
- Обновлён `README.md`: добавлены варианты blueprint, quick start, актуальные примеры и описание новых настроек develop-ветки.

## v0.2.6

- Добавлен шаг `climate.set_temperature`: целевая уставка (`target_temp`) теперь применяется явно, а не только через смену HVAC/FAN режимов.
- Усилена логика присутствия для `presence_mode=alarm`: добавлен безопасный fallback для случаев, когда выбрана не `alarm_control_panel` сущность (например `group.*`).
- Полностью актуализирован `README.md`: документ синхронизирован с реальной логикой `pvu.yaml` и оформлен в формате, пригодном для публикации статьи.

## v0.2.5

- Исправлена критичная логика булевых вычислений в шаблонах (`is_away_bool`, `is_day_bool`), из-за которой могли неверно определяться режимы.
- Добавлена устойчивая проверка поддерживаемых режимов (атрибуты `hvac_modes`/`fan_modes` climate-сущности) перед вызовами сервисов.
- Добавлена антизаморозочная защита для режима HVAC-вентиляции: новый порог `outdoor_vent_min`, ниже которого `fan_only` не включается.
- Исправлен импорт blueprint в Home Assistant: `off/maintain` в `select.options` зафиксированы как строковые значения.

## v0.2.4

- Зафиксирована конфигурация режимов под установку класса **Zenit Heco** с электрическим нагревателем и внешним охладителем: HVAC `off/fan_only/heat/cool`.
- Логика вентилятора переведена на 3 ступени (`low/medium/high`) с ограничением разгона при плохом наружном воздухе.
- В `input` для режимов добавлены `select`-поля с валидными значениями вместо свободного текста, чтобы исключить ошибочные команды.

## v0.2.1

- Проведена UI/UX-полировка `pvu.yaml`: поля в мастере Home Assistant сгруппированы по сценарию настройки (обязательные -> опциональные -> расписание -> пороги -> режимы).
- Улучшены названия и подсказки (`description`) для ключевых параметров, чтобы уменьшить вероятность ошибок при первичной настройке.
- Усилена защитная обработка опциональных сенсоров в шаблонах (`default`/безопасные строки) без изменения основной логики автоматики.
- Уточнены стартовые дефолты для более плавной работы: `outdoor_cool_min=18`, `co2_low=750`, `pm25_low=25`.
- Терминология приведена к AirGradient: используется **NOx Index** (а не абсолютный NO).

## v0.2.0

- Полностью переработан `pvu.yaml` с приоритетной логикой управления (влажность -> температура -> качество воздуха).
- Обязательными сделаны только внутренние датчики температуры и влажности; остальные датчики опциональны в любой комбинации.
- Добавлена поддержка внутренних и наружных CO2/PM2.5/VOC/NOx, гистерезиса и ограничений boost при плохом наружном воздухе.
- Обновлена документация по MQTT-сценариям Turkov и требованиям к датчикам.
